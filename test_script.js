const attachItemsToOrders = async (ordersRaw, thinky) => {
  const orders = ordersRaw.map(data => {
    // Assuming Order is available globally or passed in, but I'll write the logic directly in the file
    return data;
  });
};
